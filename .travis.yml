sudo: false

language: python

python:
  - 3.6

install:
  - pip install pandas bottle matplotlib codecov scikit-learn

script:
  - (cd test; PYTHONPATH=../src coverage run -m unittest discover climatechange)

after_success:
  - codecov
